# Build output
node_modules
dist
# .zfb (dev cache) and .zfb-build (build cache) — the scaffold's default
# gitignore only listed the exact ".zfb" name, missing the ".zfb-build"
# directory `zfb build` actually writes.
.zfb*

# macOS
.DS_Store

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Cloudflare Wrangler
.wrangler/

# zudo-doc build artifact (routes-src/ staged here at build time)
.zudo-doc/
